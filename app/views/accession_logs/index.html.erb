<%- model_class = AccessionLog -%>
<div class="page-header">
  <h1><%=t '.title', :default => AccessionLog %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:date_received) %></th>
      <th><%= model_class.human_attribute_name(:date_received_physicians_office) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @accession_logs.each do |accession_log| %>
      <tr>
        <td><%= accession_log.id %></td>
        <td><%= accession_log.date_received %></td>
        <td><%= accession_log.date_received_physicians_office %></td>
        <td><%=l accession_log.created_at, format: :long %></td>
        <td>
          <%= link_to accession_log_path(accession_log), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to accession_log_path(accession_log), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_accession_log_path,
            :class => 'btn btn-primary' %>
